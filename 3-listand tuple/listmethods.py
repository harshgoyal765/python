list =[1,2,3,4,5]
print(list.append(6))  # Adding an element to the end of the list
print(list.sort())  # Sorting the list in ascending order
print(list.sort(reverse=True))  # Sorting the list in descending order
print(list.reverse())  # Reversing the order of the list 
print(list(idx,el))  #insert an element at a specific index
list.remove(2)  # Removing the first occurrence of an element
list.pop()  # Removing the last element of the list
list.pop(idx)  # Removing an element at a specific index
#these all return none because it direct applicabel on list so wwe print list to see changes